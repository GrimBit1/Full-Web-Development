console.log("Hi, This is tutorial 690")

const express = require('express')
const app = express()

// If i only want dependencies on development not in deployment
// then we can use npm i ${module} --save-dev
